{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "PrefectHQ org baseline. Every repo extends this. Managers auto-detected; archetype presets (:python, :terraform, :go, :node, :helm, :flux) add grouping/scoping on top.",
  "extends": [
    "config:recommended",
    ":dependencyDashboard"
  ],
  "dependencyDashboardTitle": "Renovate Dependency Dashboard",
  "timezone": "America/New_York",
  "labels": [
    "automated-dependency-updates",
    "renovate"
  ],
  "commitMessagePrefix": "chore(deps):",
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0,
  "separateMinorPatch": false,
  "separateMultipleMajor": true,
  "packageRules": [
    {
      "description": "Supply-chain day-0 cooldown: wait 7 days before bumping npm and PyPI packages (easy to publish; run install/build hooks -- the main day-0 attack surface) and GitHub Actions (run in CI with secrets). Terraform providers, docker images, and go modules are left on immediate updates -- lower day-0 risk and we do not want to delay their security patches.",
      "matchDatasources": [
        "npm",
        "pypi"
      ],
      "minimumReleaseAge": "7 days"
    },
    {
      "description": "Supply-chain day-0 cooldown for GitHub Actions (see the npm/pypi rule above).",
      "matchManagers": [
        "github-actions"
      ],
      "minimumReleaseAge": "7 days"
    },
    {
      "description": "Keep each major bump as its own PR and label it. Majors are the riskiest updates and deserve isolated review.",
      "matchUpdateTypes": [
        "major"
      ],
      "addLabels": [
        "major-update"
      ]
    },
    {
      "description": "Group minor/patch bumps per ecosystem so each PR is one coherent, reviewable unit and a failure in one ecosystem does not block another. Majors are excluded (they split out per the rule above). Archetype presets add finer sub-groups on top.",
      "matchManagers": [
        "npm"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "npm dependencies"
    },
    {
      "description": "Group Go module minor/patch bumps into one PR.",
      "matchManagers": [
        "gomod"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "go modules"
    },
    {
      "description": "Group Python minor/patch bumps (pep621/uv, pip-tools, plain pip) into one PR.",
      "matchManagers": [
        "pep621",
        "pip_requirements",
        "pip-compile"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "python dependencies"
    },
    {
      "description": "Group container image minor/patch bumps (Dockerfiles and docker-compose) into one PR.",
      "matchManagers": [
        "dockerfile",
        "docker-compose"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "docker images"
    },
    {
      "description": "Group Terraform provider/module minor/patch bumps into one PR.",
      "matchManagers": [
        "terraform"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "terraform"
    },
    {
      "description": "Group Helm chart-dependency minor/patch bumps into one PR.",
      "matchManagers": [
        "helmv3"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "helm charts"
    },
    {
      "description": "Group GitHub Actions bumps into their own PR. Universal: every repo has workflows.",
      "matchManagers": [
        "github-actions"
      ],
      "groupName": "github actions"
    },
    {
      "description": "Group mise tool bumps into their own PR. Universal: every repo uses .mise.toml.",
      "matchManagers": [
        "mise"
      ],
      "groupName": "mise tools"
    }
  ]
}
